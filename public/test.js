QUnit.module("URL");


/*
asyncTest "createBlobURL", (assert)->
  _test = (str, mimetype)->
    url = createBlobURL(str, mimetype)
    $.ajax
      url: url
      success: (res)->
        assert.ok(res is str, str)
  _test("a")
  _test(new ArrayBuffer())
  expect(2)
  start()


console.log encodeURIQuery({a:"a",b:"b"})
console.log decodeURIQuery("#a=a&b=b")
console.log decodeURIQuery("#a&b=b")
console.log decodeURIQuery("#a&b=b")
 */
